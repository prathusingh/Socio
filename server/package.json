{
    "name": "socio-server",
    "version": "0.1.0",
    "private": true,
    "main": "server.js",
    "scripts": {
        "build": "rimraf dist/ && babel server.js -d dist",
        "start": "npm run build && nodemon dist/server.js"
    },
    "dependencies": {
        "body-parser": "^1.18.3",
        "express": "^4.16.4"
    },
    "devDependencies": {
        "babel-cli": "^6.26.0",
        "babel-preset-es2015": "^6.24.1",
        "nodemon": "^1.18.10"
    }
}
